# Secrets and machine-local config — every one of these has a committed .env.example
.env
*/.env
*.local

# Agent credentials kept apart from the compose-level .env (hermes/agent.env)
*/agent.env

# Runtime state
openviking/data/
allr/data/
hermes/data/
devdns/caddy/root.crt

# Editor / OS noise
*.swp
.DS_Store
