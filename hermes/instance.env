# Metadata read by ./dev.
# State is SQLite under /opt/data (bind-mounted from ./data), so this instance
# needs neither shared Postgres nor Valkey.
NEEDS_SHARED=false
DESCRIPTION="Hermes agent gateway + dashboard (upstream of allr)"
URL=https://hermes.dev.internal
