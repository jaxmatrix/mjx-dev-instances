# Metadata read by ./dev.
# Vector DB backend is `local` and QueueFS is sqlite, so no shared datastore.
NEEDS_SHARED=false
DESCRIPTION="OpenViking context DB (Gemini embedding 2 + LiteLLM VLM)"
URL=https://openviking.dev.internal
