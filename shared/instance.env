# Metadata read by ./dev. This folder *provides* the shared datastores rather
# than consuming them, so it is never itself started as a dependency of itself.
PROVIDES_SHARED=true
NEEDS_SHARED=false
DESCRIPTION="Shared Postgres 17 + Valkey 8.1"
